# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Dual-Phase-Amplitude") is currently empty — it contains only a `README.md`.

**Environment:**
- Node.js v22 and Python 3.12 are available on the VM.
- No package manager lockfiles, build configs, or dependency manifests exist yet.

**When code is added**, future agents should:
1. Re-run environment setup to install any new dependencies.
2. Update this section with service startup commands, lint/test/build instructions, and any non-obvious caveats discovered during setup.
